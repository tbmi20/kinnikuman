//Used for all menu buttons like in account and dropdowns
'use client';

export default function MenuButton () {
    return (
        <div>
            <button onClick={() => console.log('Click')}>Menu Button</button>
        </div>
    );
}